{
  "name": "illo-migracion-firestore",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Script puntual para migrar datos de Firestore a Supabase (Postgres + Auth).",
  "scripts": {
    "migrate": "node migrate-firestore.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "pg": "^8.13.0"
  }
}
