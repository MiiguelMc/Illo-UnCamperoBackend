package com.illouncampero.Backend.repository;

import com.illouncampero.Backend.model.TiendaConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TiendaConfigRepository extends JpaRepository<TiendaConfig, String> {
}
